
/**
 * Module dependencies.
 */

var stylus = require('../')
  , fs = require('fs')
  , str = fs.readFileSync('testing/test.styl', 'utf8');

stylus(str)
  .set('filename', 'testing/test.styl')
  .render(function(err, css){
    if (err) throw err;
    console.log(css);
  });